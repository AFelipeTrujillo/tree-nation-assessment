services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tree-nation-assessment-api
    ports:
      - "8080:8080"
    environment:
      APP_ENV: dev
      DATABASE_PATH: var/app.sqlite
      VISITS_PER_TREE: 5
    volumes:
      - .:/app
      - ./var:/app/var